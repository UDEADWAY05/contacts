
import { DATA_GROUP_CONTACT } from "src/__data__";
import { GroupContactsDto } from "src/types/dto/GroupContactsDto";

export function groupContactsReducer(state: GroupContactsDto[] = DATA_GROUP_CONTACT) {
    return state
}